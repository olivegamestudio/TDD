<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>Pilgrimage</RootNamespace>
    </PropertyGroup>

    <!--
      No project references by design. The quest model knows nothing about the world, the player,
      or how a game persists itself, so it has nothing to depend on.
    -->

</Project>
